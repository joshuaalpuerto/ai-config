# Output root directories per platform.
# Paths can be absolute or relative to this repository root.
#
# Example (same repo):
#   claude: .
#   github: .
#
# Example (external repo):
#   claude: ../my-project
#   github: ../my-project
#
# Example (different repos per platform):
#   claude: ../my-project
#   github: ../other-project

claude: .
github: .
